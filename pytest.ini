[pytest]
# Test discovery root: only scan the tests dir (never scan the whole repo).
testpaths = jenkins/tests
python_files = test_*.py
# The scripts dir is not a package (modules import each other by sibling
# import), so tests add it to sys.path via conftest.py, not via pythonpath
# config. Keep addopts quiet by default.
addopts = -q
